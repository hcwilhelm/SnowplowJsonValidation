SnowplowApp {
  dataSourceClassName = org.h2.jdbcx.JdbcDataSource
  dataSource {
    url = "jdbc:h2:file:./snowplowapp;DB_CLOSE_DELAY=-1;INIT=RUNSCRIPT FROM 'classpath:dbSchema.sql';MODE=postgresql"
    user = sa
  }
}